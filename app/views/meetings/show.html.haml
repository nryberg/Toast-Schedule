%span.span-4
  .span-4#header_box.meeting_background Meeting
  .span-2= link_to '(change)', edit_meeting_path(@meeting)
  .span-2= link_to '(add a meeting)', new_meeting_path
%span.span-16  
  %div.span-16#header_box= @meeting.meeting_date_pretty
  %h3.span-2 Theme:
  .span-8= @meeting.theme
  .span-16
    %h4.span-2
      Roles:
    %span.span-8
      %table
        = render :partial => 'role', :collection => @roles
        %tr
          %td{:colspan => "2"}= link_to '(+ Add Role)', new_meeting_role_path(@meeting, @role) 
  
