%span.span-4
  .span-4#lead_box.member_background Membership
%span.span-20.last
  %span.span-16
    %span.span-last#lead_box 
      %div Editing membership information for
      %div
        .loud= @membership.member.name + " in the "
      %div= @membership.club.name + " club."
    .member_opts 
      = render 'form'
