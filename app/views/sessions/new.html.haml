= content_for :top_title do
  TM Schedule Me
.span-4
  .span-4#lead_box.member_background Member
.span-20.last
  .span-20.last
    %h2 Log In
  .span-20.last
    = form_tag do
      .span-20
        .span-4#labeler Your E-Mail:
        .span-10.last
          = text_field_tag :email, params[:email]
      .span-20
        .span-4#labeler Password:
        .span-10
          = password_field_tag :password, params[:password]
      .span-20
        .span-4#labeler Remember Me:
        .span-10
          = check_box_tag :remember_me, 1, params[:remember_me] 
      .span-20.prepend-4.last
        .submit
          = submit_tag "Login"
  .span-20.prepend-4.last
    = link_to "Password help?", new_password_reset_path
