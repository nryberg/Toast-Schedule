%span.span-4
  .span-4#lead_box.club_background Club
  - if current_user.admin_for(current_club.id)
    .span-4#header_box
      = link_to '(edit)', edit_club_path(current_club)
%span-16
  %h2= current_club.name
  %h3 New Membership
  = render 'form'

= link_to 'Back', relationships_path
